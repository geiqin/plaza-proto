syntax = "proto3";
package services;
option go_package = "/.;services";


//快递发货配置
message ExpressConfig {
    string customName =1;//自定义名称
    bool isDefault =3; //设置为默认配送方式
    int32 freightConstituteRule =4;//计费方式:1-累加运费,2-组合运费
}
